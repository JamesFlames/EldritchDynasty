{
  "name": "@ed/shell",
  "version": "0.1.0",
  "private": true,
  "description": "Electron shell for Eldritch Dynasty. Wraps the editor; the game is the same code.",
  "main": "src/main.mjs",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "start": "electron .",
    "preview": "npm run build --workspace @ed/editor && electron .",
    "smoke": "electron . --smoke"
  },
  "devDependencies": {
    "electron": "^38.0.0"
  }
}
