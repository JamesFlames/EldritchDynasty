{
  "name": "@ed/client",
  "version": "0.1.0",
  "private": true,
  "description": "The game client. Reads and writes the world through GameSession and nothing else.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@ed/core": "*",
    "@ed/schema": "*",
    "vue": "^3.5.13",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.0.5",
    "vue-tsc": "^2.2.0"
  }
}
