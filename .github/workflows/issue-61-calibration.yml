name: issue-61-calibration

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  channel:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: channel oracle — 40 x 500
        run: npm run gate:blood -- 40 500 --policies=channel_oracle,concentrate,dilute
